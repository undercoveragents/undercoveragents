-# Sentinel element: auto-loads more chats when scrolled into view
#chats-load-more{ data: { controller: "intersection-load", "intersection-load-url-value": more_admin_playground_chats_path(page: pagy.next, agent_id: agent&.id, format: :turbo_stream) } }
  .playground-load-more-spinner
    %i.fa-solid.fa-spinner.fa-spin
